import math
import random
from random import randint
x = random.randint(-1, 1)
if math.sin(x) >= 0:
     k = x**2
else:
    k = abs(x);
if x<k:
    f = abs(x)
else:
    f = k*x
print(f)
print(k)
print(x)
